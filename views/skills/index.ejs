<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Express Skills</title>
    <link rel='stylesheet' href='/stylesheets/style.css'/>
</head>
<body>
    <h1>Skills</h1>
    <h3>Current Date: <%= time %> </h3>
    <a href="skills/new">New Skill</a>
    
    <ul>
      <% skills.forEach(function(skill) { %>
        <li>
          <form class="delete-form" method="POST" action="/skills/<%= skill.id %>?_method=DELETE">
            <button type='submit'>X</button>
          </form>
          <a href="/skills/<%= skill.id %>"><%= skill.skill %></a>
            - 
          <%= skill.mastered ? 'mastered' : 'not master yet' %>
        </li>
      <% }); %>
    </ul>
  </body>
</html>